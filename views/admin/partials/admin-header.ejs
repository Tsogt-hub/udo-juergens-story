<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin – <%= typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <div class="admin-layout">
    <!-- Sidebar -->
    <aside class="admin-sidebar">
      <div class="sidebar-header">
        <h2>🎵 Admin</h2>
        <p>Die Udo Jürgens Story</p>
      </div>
      <nav class="sidebar-nav">
        <a href="/admin" class="<%= typeof pageTitle !== 'undefined' && pageTitle === 'Dashboard' ? 'active' : '' %>">
          📊 Dashboard
        </a>
        <a href="/admin/termine" class="<%= typeof pageTitle !== 'undefined' && pageTitle === 'Termine' ? 'active' : '' %>">
          📅 Termine
        </a>
        <a href="/admin/bilder" class="<%= typeof pageTitle !== 'undefined' && pageTitle === 'Bilder' ? 'active' : '' %>">
          🖼️ Bilder
        </a>
        <a href="/admin/kritiken" class="<%= typeof pageTitle !== 'undefined' && pageTitle === 'Kritiken' ? 'active' : '' %>">
          💬 Kritiken
        </a>
        <a href="/admin/einstellungen" class="<%= typeof pageTitle !== 'undefined' && pageTitle === 'Einstellungen' ? 'active' : '' %>">
          ⚙️ Einstellungen
        </a>
        <div class="sidebar-divider"></div>
        <a href="/" target="_blank">🌐 Website ansehen</a>
        <a href="/admin/logout" class="sidebar-logout">🚪 Abmelden</a>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="admin-main">
      <header class="admin-topbar">
        <h1><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard' %></h1>
        <span class="admin-user">Angemeldet als <strong>Admin</strong></span>
      </header>
      <div class="admin-content">
        <%- include('../../partials/flash') %>
